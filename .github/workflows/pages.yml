name: Deploy Rainbow PWA to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment temporarily paused while upload parts are prepared."
